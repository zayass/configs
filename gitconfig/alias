[alias]
    here = "!echo $(git symbolic-ref HEAD | sed 's/refs\\/heads\\///g')"

    track = "!git branch --set-upstream-to=origin/`git here`"

    remove-upstream = "!git push origin :`git here`"
    log-local = "!git log origin/`git here`.."
